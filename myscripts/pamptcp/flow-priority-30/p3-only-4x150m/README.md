# p3-only-4x150m

## 设计目标

这个场景只保留 4 台 `prio3` 设备，每台固定 `150 Mbps`，并以 `wifi-only` 方式运行。

用途是验证一个非常具体的猜想：

- 如果 `3 x 150 Mbps` 的高时延主要因为 UL OFDMA 在 160 MHz 下只轮到 2 个 STA，
- 那么 `4 x 150 Mbps` 时，由于更容易分成 4 个等大小 RU，时延尾部应该出现明显变化。
